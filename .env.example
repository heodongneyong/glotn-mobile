NODE_ENV=production
PORT=3000

# 운영 관리자 계정
ADMIN_ID=admin
ADMIN_PASSWORD=change-this-password

# PostgreSQL 사용 시 필수
# Render/Neon/Supabase에서 제공한 연결 문자열 사용
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME

# Render postgres는 SSL 필요 (기본: 사용)
# PGSSL_DISABLE=true 로 설정하면 SSL 비활성화
